// Test stub for the `server-only` package, which throws when imported outside a
// React Server Component bundle. Aliased in vitest.config.ts so server modules
// (e.g. lib/mailer.ts) can be unit-tested in the node environment.
export {};
